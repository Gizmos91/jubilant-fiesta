<Window x:Class="CoolWindowsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoolWindowsApp"
        mc:Ignorable="d"
        Title="DevToolbox" Height="500" Width="700" Background="#F0F0F0">
    <Grid Margin="10">
        <TabControl Background="White" BorderBrush="#CCCCCC" BorderThickness="1">
            <!-- Base64 Tab -->
            <TabItem Header="Base64">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Input Text:" Margin="0,0,0,5" FontWeight="Bold"/>
                    <TextBox x:Name="Base64Input" Grid.Row="1" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                        <Button x:Name="EncodeBase64Btn" Content="Encode" Width="100" Margin="5" Click="EncodeBase64_Click"/>
                        <Button x:Name="DecodeBase64Btn" Content="Decode" Width="100" Margin="5" Click="DecodeBase64_Click"/>
                    </StackPanel>

                    <TextBlock Grid.Row="3" Text="Result:" Margin="0,0,0,5" FontWeight="Bold" VerticalAlignment="Top"/>
                    <TextBox x:Name="Base64Output" Grid.Row="3" Margin="0,25,0,0" TextWrapping="Wrap" IsReadOnly="True" VerticalScrollBarVisibility="Auto" Background="#FAFAFA"/>
                </Grid>
            </TabItem>

            <!-- Hashing Tab -->
            <TabItem Header="Hashing (SHA256)">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Input Text:" Margin="0,0,0,5" FontWeight="Bold"/>
                    <TextBox x:Name="HashInput" Grid.Row="1" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                    <Button x:Name="ComputeHashBtn" Grid.Row="2" Content="Compute SHA256 Hash" HorizontalAlignment="Center" Width="200" Margin="0,10" Click="ComputeHash_Click"/>

                    <StackPanel Grid.Row="3">
                        <TextBlock Text="SHA256 Hash:" Margin="0,0,0,5" FontWeight="Bold"/>
                        <TextBox x:Name="HashOutput" IsReadOnly="True" Background="#FAFAFA"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- JSON Formatter Tab -->
            <TabItem Header="JSON Formatter">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Raw JSON:" Margin="0,0,0,5" FontWeight="Bold"/>
                    <TextBox x:Name="JsonInput" Grid.Row="1" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas"/>

                    <Button x:Name="FormatJsonBtn" Grid.Row="2" Content="Format JSON" HorizontalAlignment="Center" Width="150" Margin="0,10" Click="FormatJson_Click"/>

                    <TextBlock Grid.Row="3" Text="Pretty JSON:" Margin="0,0,0,5" FontWeight="Bold" VerticalAlignment="Top"/>
                    <TextBox x:Name="JsonOutput" Grid.Row="3" Margin="0,25,0,0" TextWrapping="Wrap" IsReadOnly="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" Background="#FAFAFA"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
